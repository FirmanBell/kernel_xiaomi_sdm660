#
# Fingerprint driver configuration
#

if MACH_ASUS_SDM660

menuconfig INPUT_FINGERPRINT
	bool "Fingerprint"
	help
	  Say Y here, and a list of supported fingerprint will be displayed.
	  This option doesn't affect the kernel.

	  If unsure, say Y.

if INPUT_FINGERPRINT

config CDFINGER_FP
	bool "Fingerprint cdfinger"
	help
	  Say Y here if you use cdfinger fp.
	  If unsure, say N.

config CDFINGER_FP_DEBUG
	bool "Enable cdfinger debug"
	depends on CDFINGER_FP
	default n
	help
	  Say Y here if you want enable debug log for
	  cdfinger_fp driver.

config FOCALTECH_FP
	bool "Fingerprint focaltech"
	depends on MACH_ASUS_X01BD
	help
	  Say Y here if you use focaltech fp.
	  If unsure, say N.

config GOODIX_FP_X00TD
	bool "Fingerprint goodix"
	depends on MACH_ASUS_X00TD
	help
	  Say Y here if you use common fp.
	  If unsure, say N.

endif

endif
